# PASTE ME INTO THE NEXT CHAT

Copy the block below into your first message in the next chat (along with
the tarball as an upload):

---

```
Resuming inversion scrubber v4 work — 226-sample pure C. gariepinus
hatchery cohort, MS_Inversions_North_african_catfish manuscript.

Attached: scrubber_v4_turn8_handoff.tar.gz (687 KB). Please:

1. Extract it.
2. Read HANDOFF_v4_turn8_resume.md FIRST — it has the full state, what's
   queued, and the recommended plan.
3. Copy pca_scrubber_v3.html to /tmp/w2/.
4. Run the 16 test suites to confirm baseline 1,445 / 1,445 passing
   before any edits.
5. Then start with the queued ancestry-confound deliverable
   (~30 min, design already written, summarized in the handoff under
   "What's queued from prior turns" → item 1).

After that: Deliverable A (per-window row in candidate strip,
~250 lines, single turn).

DO NOT conflate the three cohorts:
- 226-sample pure C. gariepinus (THIS work)
- F1 hybrid C. gariepinus × C. macrocephalus (genome assembly paper)
- Pure C. macrocephalus wild (future paper)

Working style: compact non-fragmented pipelines, original tested code
verbatim, single central config per module, deliverables as files in
/mnt/user-data/outputs/, multi-session parallel chats — always reconcile
script versions before assuming state.
```

---

That's it — short and pointed. Claude reads the handoff for the rest.
